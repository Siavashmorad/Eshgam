services:
  eshgam-relay:
    build: .
    restart: unless-stopped
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8787
      - ESHGHAM_DATA=/app/data
